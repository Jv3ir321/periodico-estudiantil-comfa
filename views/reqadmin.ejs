<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Solicitudes de Publicación - I.E. COMFAMILIAR</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/logo2.png">
  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideInRight {
      from {
        opacity: 0;
        transform: translateX(-30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.8; }
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
    }

    .animate-slide-in {
      animation: slideInRight 0.6s ease-out forwards;
    }

    .card-hover {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .card-hover:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }

    .nav-link {
      position: relative;
      transition: all 0.3s ease;
    }

    .nav-link::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 0;
      height: 2px;
      background: white;
      transition: width 0.3s ease;
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .logo-float {
      animation: float 3s ease-in-out infinite;
    }

    .badge-pulse {
      animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    #logocabeza {
      height: 180px;
      margin-top: -40px;
      margin-bottom: -40px;
      margin-right: 85px;
      filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
    }

    .section-title {
      position: relative;
      display: inline-block;
      padding-bottom: 12px;
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 4px;
      background: linear-gradient(90deg, #10b981, #059669);
      border-radius: 2px;
    }

    .glass-effect {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
    }

    .btn-approve {
      transition: all 0.3s ease;
    }

    .btn-approve:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
    }

    .btn-reject {
      transition: all 0.3s ease;
    }

    .btn-reject:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4);
    }

    .mobile-menu {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.3s ease-out;
    }

    .mobile-menu.active {
      max-height: 500px;
      transition: max-height 0.4s ease-in;
    }

    .hamburger span {
      display: block;
      width: 25px;
      height: 3px;
      background: white;
      margin: 5px 0;
      transition: all 0.3s ease;
      border-radius: 2px;
    }

    .hamburger.active span:nth-child(1) {
      transform: rotate(45deg) translate(8px, 8px);
    }

    .hamburger.active span:nth-child(2) {
      opacity: 0;
    }

    .hamburger.active span:nth-child(3) {
      transform: rotate(-45deg) translate(7px, -7px);
    }

    .request-card {
      position: relative;
      overflow: hidden;
    }

    .request-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(16, 185, 129, 0.1), transparent);
      transition: left 0.5s;
    }

    .request-card:hover::before {
      left: 100%;
    }

    .empty-state {
      animation: fadeInUp 1s ease-out;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 font-sans">
  <script>
    function toggleMenu() {
      const menu = document.getElementById('mobile-menu');
      const hamburger = document.getElementById('hamburger');
      menu.classList.toggle('active');
      hamburger.classList.toggle('active');
    }
  </script>

  <!-- Encabezado -->
  <header class="bg-gradient-to-r from-green-800 via-green-700 to-green-800 text-white py-4 px-6 shadow-2xl">
    <div class="container mx-auto flex justify-between items-center">
      <div class="animate-slide-in">
        <h1 class="text-4xl font-bold tracking-tight">I.E. COMFAMILIAR</h1>
        <p class="text-sm mt-1 opacity-90">Panel de Administración - Solicitudes de Publicación</p>
      </div>
      <div class="items-center logo-float">
        <a href="/index" class="block transition-transform hover:scale-105">
          <img src="/logo2.png" id="logocabeza" alt="Logo Comfamiliar">
        </a>
      </div>
      <div class="text-right animate-slide-in">
        <p class="text-sm font-semibold">Año 2025 - Edición #1</p>
        <p class="text-sm mt-1 italic opacity-90">"Educación con valores para el futuro"</p>
      </div>
    </div>
  </header>

  <!-- Barra de Navegación -->
  <nav class="bg-green-700 text-white py-3 px-6 shadow-lg sticky top-0 z-50 glass-effect bg-opacity-95">
    <div class="container mx-auto">
      <div class="flex justify-between items-center">
        <!-- Desktop Menu -->
        <div class="hidden md:flex space-x-8">
          <a href="/inicio" class="nav-link font-medium hover:text-green-200">Inicio</a>
          <a href="/novedades" class="nav-link font-medium hover:text-green-200">Novedades</a>
          <a href="/trabajos" class="nav-link font-medium hover:text-green-200">Trabajos</a>
          <a href="/foro" class="nav-link font-medium hover:text-green-200">Foro</a>
          <a href="/subir-trabajos" class="nav-link font-medium hover:text-green-200">Subir Trabajos</a>
          <a href="/logout" class="nav-link font-medium hover:text-red-300 hover:font-bold">Cerrar Sesión</a>
        </div>

        <!-- User Info Desktop -->
        <div class="hidden md:flex items-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
          </svg>
          <span class="font-semibold">Bienvenido/a, <%= datos1.nomusr %></span>
        </div>

        <!-- Mobile Menu Button -->
        <button id="hamburger" class="hamburger md:hidden focus:outline-none" onclick="toggleMenu()">
          <span></span>
          <span></span>
          <span></span>
        </button>

        <!-- User Info Mobile -->
        <div class="md:hidden flex items-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
          </svg>
          <span class="font-semibold text-sm"><%= datos1.nomusr %></span>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobile-menu" class="mobile-menu md:hidden">
        <div class="flex flex-col space-y-3 pt-4 pb-2">
          <a href="/inicio" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Inicio</a>
          <a href="/novedades" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Novedades</a>
          <a href="/trabajos" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Trabajos</a>
          <a href="/foro" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Foro</a>
          <a href="/subir-trabajos" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Subir Trabajos</a>
          <a href="/logout" class="font-medium hover:text-red-300 py-2 px-4 rounded hover:bg-red-600 transition-colors">Cerrar Sesión</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Contenido Principal -->
  <main class="container mx-auto p-6">
    <!-- Banner de Bienvenida -->
    <div class="bg-gradient-to-r from-green-600 to-emerald-500 rounded-2xl shadow-xl p-8 mb-8 text-white animate-fade-in-up">
      <div class="flex items-center justify-between">
        <div>
          <h2 class="text-3xl font-bold mb-2">Panel de Moderación</h2>
          <p class="text-lg opacity-90">Revisa y aprueba las solicitudes de publicación pendientes</p>
        </div>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-24 w-24 opacity-80" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>
    </div>

    <!-- Solicitudes de Publicación -->
    <section class="mb-12">
      <h2 class="text-3xl font-bold text-gray-800 mb-6 section-title">Solicitudes Pendientes</h2>
      
      <div class="space-y-6">
        <% if (publicaciones_reqadmin && publicaciones_reqadmin.length > 0) { %>
          <% publicaciones_reqadmin.forEach((pub, index) => { %>
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden request-card card-hover animate-fade-in-up" style="animation-delay: <%= index * 0.1 %>s;">
              <div class="p-8">
                <div class="flex items-start justify-between mb-4">
                  <div class="flex-1">
                    <span class="inline-block bg-amber-100 text-amber-800 px-4 py-2 rounded-full text-sm font-semibold mb-3 badge-pulse">
                      ⏳ Pendiente de Revisión
                    </span>
                    <h3 class="text-2xl font-bold text-green-800 mb-3"><%= pub.nombre_publicacion %></h3>
                  </div>
                </div>
                
                <p class="text-gray-700 mb-6 text-lg leading-relaxed"><%= pub.contenido_publicacion %></p>
                
                <div class="flex items-center justify-between mb-6 pb-4 border-b border-gray-200">
                  <div class="flex items-center space-x-4">
                    <div class="flex items-center text-gray-600">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
                      </svg>
                      <span class="font-medium"><%= pub.autor_publicacion %></span>
                    </div>
                    <div class="flex items-center text-gray-600">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd" />
                      </svg>
                      <span><%= new Date(pub.fecha_publicacion).toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                    </div>
                  </div>
                </div>
                
                <!-- Botones de Acción -->
                <div class="flex flex-wrap gap-4">
                  <form method="POST" action="/aprobar-publicacion" class="flex-1 min-w-[200px]">
                    <input type="hidden" name="id_publicacion" value="<%= pub.id_publicacion %>">
                    <button type="submit" class="btn-approve w-full bg-gradient-to-r from-green-600 to-emerald-600 text-white px-6 py-3 rounded-xl font-semibold shadow-md hover:shadow-lg flex items-center justify-center space-x-2">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                      </svg>
                      <span>Aprobar Publicación</span>
                    </button>
                  </form>
                  
                  <form method="POST" action="/rechazar-publicacion" class="flex-1 min-w-[200px]">
                    <input type="hidden" name="id_publicacion" value="<%= pub.id_publicacion %>">
                    <button type="submit" class="btn-reject w-full bg-gradient-to-r from-red-600 to-red-700 text-white px-6 py-3 rounded-xl font-semibold shadow-md hover:shadow-lg flex items-center justify-center space-x-2">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                      </svg>
                      <span>Rechazar Publicación</span>
                    </button>
                  </form>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <div class="bg-white rounded-2xl shadow-lg p-12 text-center empty-state">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-24 w-24 mx-auto text-gray-300 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <h3 class="text-2xl font-bold text-gray-600 mb-2">¡Todo al día!</h3>
            <p class="text-gray-500 text-lg">No hay solicitudes pendientes de revisión en este momento.</p>
          </div>
        <% } %>
      </div>
    </section>

    <!-- Sección de Estadísticas -->
    <section class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-6 text-white shadow-lg card-hover">
        <div class="flex items-center justify-between">
          <div>
            <div class="text-4xl font-bold mb-2"><%= publicaciones_reqadmin ? publicaciones_reqadmin.length : 0 %></div>
            <div class="text-sm opacity-90">Solicitudes Pendientes</div>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-70" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
      <div class="bg-gradient-to-br from-green-500 to-green-600 rounded-xl p-6 text-white shadow-lg card-hover">
        <div class="flex items-center justify-between">
          <div>
            <div class="text-4xl font-bold mb-2">0</div>
            <div class="text-sm opacity-90">Aprobadas Hoy</div>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-70" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
      <div class="bg-gradient-to-br from-red-500 to-red-600 rounded-xl p-6 text-white shadow-lg card-hover">
        <div class="flex items-center justify-between">
          <div>
            <div class="text-4xl font-bold mb-2">0</div>
            <div class="text-sm opacity-90">Rechazadas Hoy</div>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-70" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
    </section>
  </main>

  <!-- Pie de Página -->
  <footer class="bg-gradient-to-r from-green-900 via-green-800 to-green-900 text-white py-8 px-6 mt-12">
    <div class="container mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-6">
        <div>
          <h3 class="text-xl font-bold mb-3">I.E. COMFAMILIAR</h3>
          <p class="text-sm opacity-80">Formando líderes con valores para construir un futuro mejor.</p>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-3">Enlaces Rápidos</h3>
          <ul class="space-y-2 text-sm">
            <li><a href="/novedades" class="hover:text-green-300 transition-colors">Novedades</a></li>
            <li><a href="/trabajos" class="hover:text-green-300 transition-colors">Trabajos</a></li>
            <li><a href="/foro" class="hover:text-green-300 transition-colors">Foro Estudiantil</a></li>
          </ul>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-3">Panel de Administración</h3>
          <p class="text-sm opacity-80">Gestiona las publicaciones de manera eficiente</p>
          <p class="text-sm opacity-80 mt-2">y mantén la calidad del contenido</p>
        </div>
      </div>
      <div class="border-t border-green-700 pt-6 text-center">
        <p class="text-sm opacity-80">© 2025 Periódico Escolar - I.E. COMFAMILIAR. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</body>
</html>
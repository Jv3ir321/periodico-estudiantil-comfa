<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Foro Estudiantil - I.E. COMFAMILIAR</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/logo2.png">
  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideInRight {
      from {
        opacity: 0;
        transform: translateX(-30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
    }

    .animate-slide-in {
      animation: slideInRight 0.6s ease-out forwards;
    }

    .card-hover {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .card-hover:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 32px rgba(0, 0, 0, 0.15);
    }

    .nav-link {
      position: relative;
      transition: all 0.3s ease;
    }

    .nav-link::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 0;
      height: 2px;
      background: white;
      transition: width 0.3s ease;
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .logo-float {
      animation: float 3s ease-in-out infinite;
    }

    #logocabeza {
      height: 180px;
      margin-top: -40px;
      margin-bottom: -40px;
      margin-right: 85px;
      filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
    }

    .section-title {
      position: relative;
      display: inline-block;
      padding-bottom: 12px;
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 4px;
      background: linear-gradient(90deg, #10b981, #059669);
      border-radius: 2px;
    }

    .btn-submit {
      position: relative;
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .btn-submit::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      background: rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }

    .btn-submit:hover::before {
      width: 300px;
      height: 300px;
    }

    .btn-submit:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(5, 150, 105, 0.4);
    }

    .btn-delete {
      transition: all 0.3s ease;
    }

    .btn-delete:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(220, 38, 38, 0.4);
    }

    .btn-edit {
      transition: all 0.3s ease;
    }

    .btn-edit:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(37, 99, 235, 0.4);
    }

    /* Responsive Menu */
    .mobile-menu {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.3s ease-out;
    }

    .mobile-menu.active {
      max-height: 500px;
      transition: max-height 0.4s ease-in;
    }

    .hamburger span {
      display: block;
      width: 25px;
      height: 3px;
      background: white;
      margin: 5px 0;
      transition: all 0.3s ease;
      border-radius: 2px;
    }

    .hamburger.active span:nth-child(1) {
      transform: rotate(45deg) translate(8px, 8px);
    }

    .hamburger.active span:nth-child(2) {
      opacity: 0;
    }

    .hamburger.active span:nth-child(3) {
      transform: rotate(-45deg) translate(7px, -7px);
    }

    .glass-effect {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
    }

    .empty-state {
      animation: fadeInUp 0.8s ease-out;
    }

    .post-card {
      background: linear-gradient(135deg, #ffffff 0%, #f9fafb 100%);
    }

    .post-icon {
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, #10b981, #059669);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 14px;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

    .author-avatar {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, #10b981, #059669);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 16px;
    }

    .form-input {
      transition: all 0.3s ease;
    }

    .form-input:focus {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 font-sans">
  <script>
    function toggleMenu() {
      const menu = document.getElementById('mobile-menu');
      const hamburger = document.getElementById('hamburger');
      menu.classList.toggle('active');
      hamburger.classList.toggle('active');
    }
  </script>

  <!-- Encabezado -->
  <header class="bg-gradient-to-r from-green-800 via-green-700 to-green-800 text-white py-4 px-6 shadow-2xl">
    <div class="container mx-auto flex justify-between items-center">
      <div class="animate-slide-in">
        <h1 class="text-4xl font-bold tracking-tight">I.E. COMFAMILIAR</h1>
        <p class="text-sm mt-1 opacity-90">Foro Estudiantil</p>
      </div>
      <div class="items-center logo-float">
        <a href="/inicio" class="block transition-transform hover:scale-105">
          <img src="logo2.png" id="logocabeza" alt="Logo Comfamiliar">
        </a>
      </div>
      <div class="text-right animate-slide-in hidden md:block">
        <p class="text-sm font-semibold">Año 2025 - Edición #1</p>
        <p class="text-sm mt-1 italic opacity-90">"Educación con valores para el futuro"</p>
      </div>
    </div>
  </header>

  <!-- Barra de Navegación -->
  <nav class="bg-green-700 text-white py-3 px-6 shadow-lg sticky top-0 z-50 glass-effect bg-opacity-95">
    <div class="container mx-auto">
      <div class="flex justify-between items-center">
        <!-- Desktop Menu -->
        <div class="hidden md:flex space-x-8">
          <a href="/inicio" class="nav-link font-medium hover:text-green-200">Inicio</a>
          <a href="/novedades" class="nav-link font-medium hover:text-green-200">Novedades</a>
          <a href="/trabajos" class="nav-link font-medium hover:text-green-200">Trabajos</a>
          <a href="/foro" class="nav-link font-medium hover:text-green-200">Foro</a>
          <a href="/subir-trabajos" class="nav-link font-medium hover:text-green-200">Subir Trabajos</a>
          <a href="/logout" class="nav-link font-medium hover:text-red-300 hover:font-bold">Cerrar Sesión</a>
        </div>

        <!-- User Info Desktop -->
        <div class="hidden md:flex items-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
          </svg>
          <span onclick="window.location.href='/usuarios/<%= datos1.nomusr %>'" class="font-semibold hover:underline">Bienvenido/a, <%= datos1.nomusr %></span>
        </div>

        <!-- Mobile Menu Button -->
        <button id="hamburger" class="hamburger md:hidden focus:outline-none" onclick="toggleMenu()">
          <span></span>
          <span></span>
          <span></span>
        </button>

        <!-- User Info Mobile -->
        <div class="md:hidden flex items-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
          </svg>
          <span onclick="window.location.href='/usuarios/<%= datos1.nomusr %>'" class="font-semibold hover:underline">Bienvenido/a, <%= datos1.nomusr %></span>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobile-menu" class="mobile-menu md:hidden">
        <div class="flex flex-col space-y-3 pt-4 pb-2">
          <a href="/inicio" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Inicio</a>
          <a href="/novedades" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Novedades</a>
          <a href="/trabajos" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Trabajos</a>
          <% if (datos1.rolusr === "Administrador") { %>
          <a href="/reqadmin" class="nav-link font-medium hover:text-green-200">Peticiones</a>
          <% } %>
          <a href="/foro" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Foro</a>
          <a href="/subir-trabajos" class="font-medium hover:text-green-200 py-2 px-4 rounded hover:bg-green-600 transition-colors">Subir Trabajos</a>
          <a href="/logout" class="font-medium hover:text-red-300 py-2 px-4 rounded hover:bg-red-600 transition-colors">Cerrar Sesión</a>
          <span onclick="window.location.href='/usuarios/<%= datos1.nomusr %>'" class="font-semibold hover:underline">Bienvenido/a, <%= datos1.nomusr %></span>

        </div>
      </div>
    </div>
  </nav>

  <!-- Contenido Principal -->
  <main class="container mx-auto p-6">
    <!-- Banner de Bienvenida -->
    <div class="bg-gradient-to-r from-green-600 to-emerald-500 rounded-2xl shadow-xl p-8 mb-8 text-white animate-fade-in-up">
      <div class="flex items-center justify-between">
        <div>
          <h2 class="text-3xl font-bold mb-2 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z" />
            </svg>
            Foro de Discusión
          </h2>
          <p class="text-lg opacity-90">Comparte ideas, debates y conecta con la comunidad estudiantil</p>
        </div>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-24 w-24 opacity-80 hidden lg:block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
        </svg>
      </div>
    </div>

    <!-- Formulario Nuevo Tema -->
    <div class="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-8 animate-fade-in-up" style="animation-delay: 0.1s;">
      <h3 class="text-2xl font-bold text-green-800 mb-6 flex items-center section-title">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7 mr-2 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
        </svg>
        Crear Nuevo Tema
      </h3>

      <form id="form" method="post" action="/enviar">
        <div class="mb-6">
          <label class="block text-gray-700 font-bold mb-3 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 20l4-16m2 16l4-16M6 9h14M4 15h14" />
            </svg>
            Título del Tema
          </label>
          <input 
            type="text" 
            class="form-input w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
            id="title" 
            name="titulo"
            placeholder="Escribe un título descriptivo..."
            required>
        </div>

        <div class="mb-6">
          <label class="block text-gray-700 font-bold mb-3 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
            </svg>
            Contenido del Mensaje
          </label>
          <textarea 
            class="form-input w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
            rows="5" 
            id="content" 
            name="mensaje"
            placeholder="Comparte tus ideas, preguntas o comentarios..."
            required></textarea>
        </div>

        <button 
          type="submit" 
          class="btn-submit w-full md:w-auto bg-gradient-to-r from-green-600 to-green-700 text-white px-8 py-3 rounded-xl hover:from-green-700 hover:to-green-800 font-semibold shadow-lg flex items-center justify-center relative z-0">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
          </svg>
          Publicar Tema
        </button>
      </form>
    </div>

    <!-- Listado de Publicaciones -->
    <section id="posts" class="animate-fade-in-up" style="animation-delay: 0.2s;">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-3xl font-bold text-gray-800 section-title">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 inline-block mr-2 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
          </svg>
          Temas de Discusión
        </h2>
      </div>

      <div class="space-y-6">
        <% if (publicaciones && publicaciones.length > 0) { %>
          <% publicaciones.forEach((pub, index) => { %>
            <article class="post-card rounded-2xl shadow-lg overflow-hidden card-hover" style="animation: fadeInUp 0.6s ease-out <%= index * 0.05 %>s forwards; opacity: 0;">
              <div class="p-6 md:p-8">
                <!-- Header -->
                <div class="flex items-start gap-6 mb-6">
                  <div class="post-icon flex-shrink-0">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z" />
                    </svg>
                  </div>
                  <div class="flex-1">
                    <a href="/foro/<%= pub.nombre_publicacion %>" class="text-2xl md:text-3xl font-bold text-green-800 mb-3 hover:text-green-600 transition-colors block">
                      <%= pub.nombre_publicacion %>
                    </a>
                  </div>
                </div>

                <!-- Contenido -->
                <div class="mb-6 ml-0 md:ml-20">
                  <p class="text-gray-700 text-lg leading-relaxed"><%= pub.contenido_publicacion %></p>
                </div>

                <!-- Footer -->
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 pt-6 border-t border-gray-200 ml-0 md:ml-20">
                  <!-- Autor y Fecha -->
                  <div class="flex items-center gap-4">
                    <div class="author-avatar">
                      <%= pub.autor_publicacion.charAt(0).toUpperCase() %>
                    </div>
                    <div>
                      <p class="font-semibold text-gray-800"><%= pub.autor_publicacion %></p>
                      <p class="text-sm text-gray-500 flex items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                        <%= new Date(pub.fecha_publicacion).toLocaleDateString('es-ES', { 
                          year: 'numeric', 
                          month: 'long', 
                          day: 'numeric' 
                        }) %>
                      </p>
                    </div>
                  </div>

                  <!-- Acciones -->
                  <% if (datos1.rolusr === "Administrador" || datos1.nomusr === pub.autor_publicacion) { %>
                    <div class="flex gap-3 w-full md:w-auto">
                      <form method="POST" action="/editar-post" class="flex-1 md:flex-none">
                        <input type="hidden" name="id_publicacion" value="<%= pub.id_publicacion %>">
                        <input type="hidden" name="titulo_actual" value="<%= pub.nombre_publicacion %>">
                        <input type="hidden" name="contenido_actual" value="<%= pub.contenido_publicacion %>">
                        <button 
                          type="submit" 
                          class="btn-edit w-full bg-blue-600 text-white px-6 py-3 rounded-xl hover:bg-blue-700 font-semibold shadow-lg flex items-center justify-center">
                          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                          </svg>
                          Editar
                        </button>
                      </form>

                      <form method="POST" action="/eliminar" class="flex-1 md:flex-none">
                        <input type="hidden" name="id_publicacion" value="<%= pub.id_publicacion %>">
                        <input type="hidden" name="autor_publicacion" value="<%= pub.autor_publicacion %>">
                        <button 
                          type="submit" 
                          class="btn-delete w-full bg-red-600 text-white px-6 py-3 rounded-xl hover:bg-red-700 font-semibold shadow-lg flex items-center justify-center" 
                          onclick="return confirm('¿Estás seguro de eliminar esta publicación?')">
                          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                          </svg>
                          Eliminar
                        </button>
                      </form>
                    </div>
                  <% } %>
                </div>
              </div>
            </article>
          <% }) %>
        <% } else { %>
          <div class="bg-white rounded-2xl shadow-lg p-16 text-center empty-state">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-32 w-32 mx-auto text-gray-300 mb-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
            </svg>
            <h3 class="text-3xl font-bold text-gray-400 mb-3">No hay publicaciones aún</h3>
            <p class="text-gray-500 text-lg mb-6">¡Sé el primero en iniciar una conversación!</p>
            <button 
              onclick="document.getElementById('title').focus()" 
              class="inline-flex items-center bg-gradient-to-r from-green-600 to-green-700 text-white px-8 py-3 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
              </svg>
              Crear Primer Tema
            </button>
          </div>
        <% } %>
      </div>
    </section>
  </main>

  <!-- Pie de Página -->
  <footer class="bg-gradient-to-r from-green-900 via-green-800 to-green-900 text-white py-8 px-6 mt-12">
    <div class="container mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-6">
        <div>
          <h3 class="text-xl font-bold mb-3">I.E. COMFAMILIAR</h3>
          <p class="text-sm opacity-80">Formando líderes con valores para construir un futuro mejor.</p>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-3">Recursos</h3>
          <ul class="space-y-2 text-sm">
            <li><a href="/trabajos" class="hover:text-green-300 transition-colors">Biblioteca de Trabajos</a></li>
            <li><a href="/subir-trabajos" class="hover:text-green-300 transition-colors">Subir Trabajo</a></li>
            <li><a href="/foro" class="hover:text-green-300 transition-colors">Foro Estudiantil</a></li>
          </ul>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-3">Contacto</h3>
          <p class="text-sm opacity-80">¿Necesitas ayuda?</p>
          <p class="text-sm opacity-80 mt-2">Contacta al soporte técnico</p>
        </div>